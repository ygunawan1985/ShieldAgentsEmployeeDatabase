<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shieldagentsemployeedatabase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EmployeeListActivity"
            android:label="@string/title_activity_employee_list"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".EmployeeDetailsActivity" />
        <activity android:name=".entity.EmployeeListActivity" />
        <activity android:name=".UpdateEmployeeActivity" />
        <activity android:name=".DeleteEmployeeActivity" />
        <activity android:name=".NewEmployeeActivity" />
        <activity android:name=".FilterEmployeeActivity" />
        <activity android:name=".ViewDepartment" />
        <activity android:name=".ViewDepartments" />
        <activity android:name=".AddDepartmentActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>